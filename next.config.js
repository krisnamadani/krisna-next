/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'www.google.com', 'images.unsplash.com', 'flowbite.s3.amazonaws.com'],
    },
}

module.exports = nextConfig
